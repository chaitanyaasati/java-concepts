# Observer Design Pattern

implementation covers the core Observer pattern well — the separation of Publisher (event infrastructure) from HindustanTimes (concrete subject) via composition
is a good design choice.